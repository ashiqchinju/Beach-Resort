import React from 'react'

const Title = ({ title }) => {
    return (
        <div className="section-list">
            <h4>{title}</h4>
            <div />
        </div>
    )
}

export default Title